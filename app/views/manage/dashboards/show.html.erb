<div class="manage_header">
	<h2>Activities for you</h2>
</div>
<section>
	<ul id="activities">
		<% @activities.each do |activity| %>
			<%= render "activities/shared/#{activity.action.underscore}", :activity => activity %>
		<% end %>
	</ul>
</section>